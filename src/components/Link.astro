---
import { cn } from '../lib/utils';

export interface Props {
  class?: string;
}

const { class: className = "", ...rest } = Astro.props;
---
<a class={cn('text-primary underline font-simplify font-medium', className)} {...rest}>
    <slot />
</a>
