---
export interface Props {
    title?: string;
    description?: string;
    class?: string;
}

const { title, description, class: additionalClass = "" } = Astro.props;
---

<div class={`bg-white shadow-sm rounded-2xl border border-secondary-100 ${additionalClass}`}>
    <div class="px-4 py-5 sm:p-6">
        {(title || description) && (
            <div class="flex flex-col gap-4 sm:flex-row sm:items-start sm:justify-between mb-4">
                <div>
                    {title && (
                        <h3 class="text-lg leading-6 text-secondary-900 font-simplify">
                            {title}
                        </h3>
                    )}
                    {description && (
                        <div class="mt-2 max-w-xl text-sm text-secondary-500">
                            <p>{description}</p>
                        </div>
                    )}
                </div>
                <slot name="actions" />
            </div>
        )}
        <slot />
    </div>
</div>