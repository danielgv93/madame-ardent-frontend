---
export interface Props {
    href?: string;
    label: string;
    dataPage?: string;
    isDisabled?: boolean;
}

const { href, label, dataPage, isDisabled = false } = Astro.props;
---

<li>
    {isDisabled ? (
        <div class="flex items-center px-3 py-2 text-sm font-medium rounded-md text-secondary-400 cursor-not-allowed">
            <slot name="icon" />
            {label}
        </div>
    ) : (
        <a href={href}
           class="dashboard-nav-link flex items-center px-3 py-2 text-sm font-medium rounded-md text-secondary-600 hover:text-primary hover:bg-primary-50 transition-colors duration-150"
           data-page={dataPage}>
            <slot name="icon" />
            {label}
        </a>
    )}
</li>