---
import { cn } from '../lib/utils';

export interface Props {
  class?: string;
}

const { class: className = "" } = Astro.props;
---
<h1 class={cn('text-primary text-6xl font-simplify', className)}>
    <slot />
</h1>
