---
import { cn } from '../lib/utils';

export interface Props {
  class?: string;
}

const { class: className = "" } = Astro.props;
---

<div class={cn('w-full h-full flex flex-row gap-4 justify-center items-center', className)}>
  <slot />
</div>
